import HeroText from "./HeroText";

const Hero = () => {
  return (
    <section className="w-full h-screen flex items-center justify-center border shadow-lg"><HeroText/></section>
  )
}

export default Hero;